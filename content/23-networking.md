---
slug: networking
order: 23
number: '21'
group: BACKEND & DATA
title: Networking
status: questions-only
---

## Can you explain AJAX in detail, and what are its advantages and disadvantages? (M)

## What’s the difference between XMLHttpRequest and fetch()? (M)

### Does fetch reject on a 404? What does that mean for your error handling?

## How do you abort a request with AbortController? (M)

## What is CORS, how is it configured, and what triggers a preflight? (M)

### Which requests are simple and skip the preflight?

### Why does a wildcard origin fail once credentials are involved?

### Is CORS protecting the server or the user?

## What are the main HTTP methods, which are idempotent, and what do the common status codes mean? (M)

### Is POST idempotent? Is PATCH?

### When would you return 202, 409 or 422?

## What do the HTTP caching headers (Cache-Control, ETag, If-None-Match) do? (M)

### What is the difference between no-cache and no-store?

### What does stale-while-revalidate buy you?

## What’s the difference between WebSockets, Server-Sent Events and polling? (M)

### Which survives a proxy that buffers responses, and which reconnects automatically?

## What are the high-level differences between HTTP/1.1, HTTP/2 and HTTP/3? (M)

### What is head-of-line blocking, and which version solves it at which layer?

### Does HTTP/2 make domain sharding and bundling obsolete?

## What’s the difference between a forward proxy and a reverse proxy? (M)

## How does a CDN actually reduce latency for a user on the other side of the world? (M)
